#include <stdio.h>
#include <raylib.h>

int main () {

  while(!WindowShouldClose()){

    InitWindow(1200,900,"Stupid Maze");
    SetFPS(60);
    BeginDrawing();
    ClearBackground(BLACK);
    DrawText("hello",10,10,10,BLACK);
    
    
  }
  CloseWindow();
  return 0;
}
